# 行情数据链路

[Wiki 首页](README.md) · [系统架构](01-architecture.md) · [功能地图](02-feature-map.md)

## 数据源总览

桌面版网络请求集中在 `electron/main`：

| 数据 | 主进程函数 | 上游 |
| --- | --- | --- |
| 股票搜索 | `searchStocks` | 东方财富搜索建议 |
| 批量报价 | `fetchQuotes` | 东方财富批量行情 |
| 五档盘口 | `fetchOrderBook` | 东方财富个股行情 |
| 当日异动 | `fetchTodayRadarSignals` | 东方财富异动 |
| 近 5 日异动 | `fetchHistoricalRadarSignals` | 东方财富异动统计与明细 |
| 分时/五日/周期 K | `fetchKline` | 东方财富趋势/K 线 |
| 所属板块和板块报价 | `fetchSectorBinding/Quotes/Index` | 东方财富个股页与板块行情 |
| 资金流向 | `fetchFundsFlow` | 东方财富分钟资金流 |
| 休市日历 | `fetchSseTradingCalendar` | 上交所休市安排 |

所有行情请求通过 Electron `net.fetch` 发出。`requestJson` 使用 12 秒超时并最多尝试两次；板块页面文本请求使用同样的 12 秒超时，但不重试。

## 报价标识

代码使用东方财富 `quoteId` 作为跨模块主键，例如：

```text
1.600519   沪市贵州茅台
0.300750   深市宁德时代
90.BK0896  行业板块
```

`WatchStock.quoteId`、`StockQuote.quoteId`、`TTradingAccounts` 的键和各缓存键都依赖它。新增数据源时应先统一到现有 `quoteId`，不要在 UI 层再按股票代码猜市场。

## 实时报价

`fetchQuotes` 批量请求以下字段并映射到 `StockQuote`：

| `StockQuote` 字段 | 含义 |
| --- | --- |
| `latest` | 最新价 |
| `change` | 涨跌额 |
| `changePercent` | 涨跌幅 |
| `open/high/low/previousClose` | 今开、最高、最低、昨收 |
| `volume` | 成交量 |
| `amount` | 成交额 |
| `turnoverRate` | 换手率 |
| `sector` | 主行业板块简要行情，后续补充 |
| `radarSignals` | 已缓存的近 5 日异动 |
| `updatedAt` | 本次转换时间 |

东方财富部分价格/百分比字段按 100 倍整数返回，`scaled` 统一除以 100；成交量和成交额使用原值。

### 主进程合并

`refreshStocks` 并行获取：

1. 当前股票报价。
2. 可选大盘指数报价。
3. 股票所属板块报价。

然后 `mergeQuotes`：

- 用 `quoteId` 覆盖旧报价。
- 新报价没有板块信息时保留旧板块。
- 最终只保留当前自选和已选择的大盘指数。

网络失败时不会清空已有行情，而是广播 `data:error` 并返回最近一次 `latestQuotes`。

## 自动刷新

主表刷新由 Electron 主进程驱动；详情面板各自维护定时刷新。

| 对象 | 默认间隔 | 刷新位置 |
| --- | --- | --- |
| 重点股票 | 5 秒 | Electron 主进程 |
| 普通股票和大盘指数 | 10 秒 | Electron 主进程 |
| 分时、五日、盘口、资金流、板块 | 跟随该股票重点/普通间隔 | React 面板 |
| 日/周/月 K | 缓存 5 分钟，主要按需补历史 | React 面板 |

所有自动刷新统一受 `shared/market-hours.ts` 的北京时间窗口限制。手动刷新不受限制。

## 分时和 K 线

### 类型

`KlinePeriod`：

```ts
'intraday' | 'fiveDay' | 'daily' | 'weekly' | 'monthly'
```

统一返回 `KlineResult`，每条 `KlineBar` 包含：

```text
time, open, close, high, low, volume, amount
```

### 请求策略

| 周期 | 上游粒度 | 初始数量 |
| --- | --- | --- |
| 分时 | 当日趋势 | 最近交易日 |
| 五日 | 5 分钟 K | 240 条 |
| 日 K | 日线 | 120 条 |
| 周 K | 周线 | 104 条 |
| 月 K | 月线 | 60 条 |

分时趋势接口失败时，自动请求 120 条 5 分钟 K，并只保留最后交易日作为备用分时。

日/周/月向左缩放到早期数据附近时，`ExpandedStockDetails` 将请求数量翻倍，直到：

- 日 K：最多 1920 条。
- 周 K：最多 1664 条。
- 月 K：最多 960 条。

### 图表实现

`CandlestickChart.tsx` 用于分时、五日和板块分时：

- 分时固定补齐 09:15–11:30、13:00–15:00 时间槽。
- 集合竞价和连续竞价使用不同线条/背景语义。
- 当日均价按累计成交额 ÷ 累计成交股数计算，成交量按“手 × 100 股”换算。
- 标记连续竞价最高和最低。
- 分时价格和成交量为两个同步但独立的图表。
- 五日图把成交量叠在价格图下部。

`PeriodKlineChart.tsx` 用于日/周/月：

- 红涨绿跌蜡烛。
- 成交量叠加在底部。
- 最新周期固定在右侧。
- 只允许缩放，不允许横向滚动离开最新端。
- 根据当前可见范围重新标记最高和最低。

## 五档盘口

`fetchOrderBook` 返回：

- 卖一到卖五。
- 买一到买五。
- 最新价、昨收和更新时间。

上游字段顺序在 `market.ts` 中显式映射；`OrderBookPanel` 再将卖盘反转为界面常见的“卖五 → 卖一 → 最新 → 买一 → 买五”顺序。

盘口在分时标签右侧展示，按股票刷新间隔更新。收盘后所有档位为空时显示“收盘后暂无挂单”。

## 资金流向

`fetchFundsFlow` 读取最近交易日的分钟累计值：

- 主力净额。
- 超大单净额。
- 大单净额。
- 中单净额。
- 小单净额。

其中超大单由“主力 - 大单”推算。面板展示最新汇总、曲线和最近 8 个时间点。

## 所属行业板块

板块绑定从个股页面中的 `quotedata` 读取 `bk_id` 和 `bk_name`，缓存在主进程内 24 小时。

主表刷新时，`fetchSectorQuotes`：

1. 获取每只股票的板块绑定。
2. 对板块去重。
3. 批量请求板块报价。
4. 把板块涨跌幅映射回个股。

板块详情页同时请求板块最新报价和分时，只显示 09:30 之后的连续竞价走势。

## 异动缓存

| 缓存 | 有效期 | 说明 |
| --- | --- | --- |
| 当日异动 | 30 秒 | 一次请求全市场后筛选自选股 |
| 历史异动 | 10 分钟 | 按股票分批获取近 5 日统计与明细 |
| 板块绑定 | 24 小时 | `stockQuoteId → board` |

历史异动按 6 只股票一批并行处理，批次之间串行，降低瞬时请求量。

## React 面板缓存

分时、K 线、盘口、资金流和板块都使用组件文件内的模块级 `Map`。特点：

- 在展开/收起或切换股票后仍可复用。
- 只存在于当前渲染进程内。
- 应用重启后丢失。
- 请求失败但存在旧数据时，继续展示旧数据并显示刷新警告。

## 浏览器演示数据

`src/lib/api.ts` 在没有 `window.stockApi` 时启用 `demoApi`：

- 内置常用股票和指数。
- 生成报价、K 线、盘口、资金流和板块数据。
- 使用 `localStorage` 保存演示状态。
- 配置导出通过浏览器下载。
- 交易日历在线刷新不可用。

新增 IPC 数据能力时必须给 `demoApi` 补实现，否则 `npm run dev:web` 预览会缺功能或类型无法通过。

## 已知边界

- 上游是公开接口，没有稳定性承诺；字段、令牌或页面结构调整会直接影响 `market.ts`。
- 没有行情落库、断点补偿或历史快照；应用只保留当前内存报价。
- `AppTitlebar` 的“交易中/已休市”只按工作日和常规交易时间判断，不读取法定休市日历；自动刷新窗口也只判断时段。
- 各详情面板独立刷新，展开多只股票或频繁切换时会增加请求量。

