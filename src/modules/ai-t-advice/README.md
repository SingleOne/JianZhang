# AI 做 T 参考模块骨架

该模块默认不构建，只有同时设置 `JIANZHANG_AI_MODULE=1` 与 `JIANZHANG_AI_T_ADVICE_MODULE=1` 时才会安装。当前仅保留独立删除边界，不包含提示词、交易建议、价格/数量逻辑、T 计划写命令或 UI 入口。

未来若专项合规评估通过，所有做 T 参考的类型、提示词、校验、存储、IPC 和界面都必须只放在该目录及其薄安装点中；删除此目录不影响基础 AI 对话、指标解读和对话记录。
