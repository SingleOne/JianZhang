# 项目协作规则

- 不要过度进行防御性编程。
- 所有界面文字大小不得低于 `12px`，包括提示、说明和辅助信息。
- 所有股票数量相关的数字输入框，`step` 必须统一设置为 `100`。
- 所有收益或收益率数字必须根据正负显示颜色：正数显示红色，负数显示绿色，零值使用中性色。
- 功能开发完成后不需要进行界面验证，应主动执行一次构建，并以构建结果作为完成检查。
- 每次打包前进行代码提交，提交时根据变更内容填充message
- 每次打包时，如果只是小修小改，版本号尾数+1；如果是新增了某个小功能，版本号中间数+1，并且位数归0；如果检测到新功能模块，提醒我是否需要更新大版本号
